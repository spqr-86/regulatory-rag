# Шаг 1: q1+q2 на Sonnet 5 — 2026-09-16

Модель `anthropic/claude-sonnet-5` через OpenRouter, temperature 0, режим `v2`, answer prompt
`department_answer` v4, verify prompt v1, `chroma_db_dept_v2` / `department_demo_v2`, 1115 чанков —
та же конфигурация, что `object_profile_verify_2026-09-16`, кроме модели. Запуск:
`--only 1,2 --model anthropic/claude-sonnet-5`. 2 answer-вызова, 0 schema-повторов, 0 verify-вызовов
(оба ответа не `answered`).

Usage: 16 002 input / 11 605 output токенов. По прайсу OpenRouter на 16.09 ($2 / $10 за 1M) —
около $0.15. Счёт OpenRouter не сверялся.

| | gpt-4o-mini (прогон 16.09) | Sonnet 5 |
|---|---|---|
| q1 статус | `needs_review/verification_contradiction` | `needs_context/applicability_unclear` — основной ожидаемый |
| q1 подответы | — | 4/4 |
| q1 запрещённые выводы | 1 (план нужен из-за 8 человек) | 0 |
| q2 статус | `answered` | `needs_context/applicability_unclear` — допустимая альтернатива (спрашивает площади) |
| q2 подответы | — | 2/4 |
| q2 запрещённые выводы | 0 | 0 |

## Выводы

- **Смысловая ошибка q1 — ошибка модели, а не подхода.** Sonnet 5 не применил порог к 8 людям офиса,
  назвал недостающие величины (люди в здании, постоянные рабочие места на этаже) и задал их в
  clarifying_questions. Это ровно тот `missing`, которого не дал verifier на mini.
- q1, спорное место: applied conclusion «2 огнетушителя в зоне офиса соответствуют минимуму на этаж» —
  этаж бизнес-центра шире зоны офиса. В запрещённые не входит, но вывод натянут.
- **q2 теряет 2 подответа из-за retrieval, не модели.** П. 405 (50 % при АУГП) в промпт не попал
  вообще; `ext_006` — обрывок таблицы приложения № 1 без категорий, модель взяла из него ранг
  `10A, 233B, C, E` (строка другой категории) вместо `55В, С, Е`. Лечится нарезкой таблиц и поиском.
- Выборка — 2 вопроса, доказательство слабое. Следующее: полный набор 9 на Sonnet 5 (~$0.7 по
  этой пропорции) или отдельно чинить нарезку таблицы ПП № 1479.
