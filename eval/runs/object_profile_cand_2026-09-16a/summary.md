# Прогон кандидатов 16.09: q1×2 + q7 на deepseek/deepseek-v4.1-flash (v2)

Прогоны: `object_profile_cand_2026-09-16a` (q1, q7), `object_profile_cand_2026-09-16b` (q1).
Токены: a — 8979 in / 14346 out (основная часть выхода на q7), b — 5401 / 2327.

| прогон | q | статус / reasons | contract_ok | нарушения оценщика |
|---|---:|---|---|---|
| a | 1 | needs_context / applicability_unclear | да | applied_on_unknown |
| a | 7 | needs_context / applicability_unclear | да | — (все `obj_f_*` unmapped) |
| b | 1 | needs_context / applicability_unclear | да | applied_on_unknown |

- Ловушка q1 пройдена оба раза: 8 человек в зоне под порог 50 не подставлены, модель пишет,
  что численность на этаже/в здании и рабочие места неизвестны → безусловного вывода нет.
- `applied_on_unknown` на q1 — вероятно, ложное срабатывание оценщика: текст вывода условный
  («нужен, если применим порог»). Не проверено, какой тип утверждения в `q1.json`.
- q7: все `obj_f_*` unmapped — вероятно, в expectations q7 нет `typed_fields`. Не проверено.
- Гейт в `decide()` не сработал: уточняющий вопрос раньше (по дизайну).
- Оценщик не умеет частичный прогон (требует все 9 вопросов) — оценка шла по ожиданиям,
  отфильтрованным до q1/q7 во временном файле.
