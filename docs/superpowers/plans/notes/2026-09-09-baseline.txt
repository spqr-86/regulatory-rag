Triage decision contract — test baseline
Captured: 2026-09-10 (Europe/Berlin)
Commit: 025beb1620be9c67b7f5bc7be71c2db2fd8e35b8
Runner A: Codex sandbox with syscall/network isolation
Runner B: Codex command executed outside the sandbox after explicit approval
Command (both runners): .venv/bin/pytest -q
Python: 3.13.5
pytest: 9.1.1

Comparison rule: treat a later result as a regression only when commit ancestry,
test command, network isolation, and Python/pytest versions are compatible with
the conditions above. Otherwise first recapture the baseline for that environment.

FULL-SUITE STATUS: COMPLETE. The plan's five-failure baseline holds after
commit 025beb1 isolated tests from developer-machine embedding configuration.

Runner A cannot execute Starlette TestClient tests: its syscall filter rejects
send() on an AF_UNIX socketpair with EPERM. That prevents
asyncio.call_soon_threadsafe() from waking the event loop. The hang reproduces with
a minimal FastAPI app and is not caused by api.py or the RAG pipeline.

Runner B completed in 42.99 seconds:
  881 passed, 5 failed, 7 warnings (886 collected)

The five baseline failures comprise:
  - 2 known visual-proof failures in tests/test_agent_tools.py;
  - 3 known FlashRank adapter failures in tests/v7/test_bridge_rerank.py;

Before commit 025beb1, nine additional tests inherited
V7_DOMAIN_GATE_THRESHOLD=0.25 and the OpenAI embedding provider from .env, made
real network calls, and failed with 429 insufficient_quota. The shared pytest
fixture now disables the runtime domain gate by default, while the real-Chroma
integration test injects DeterministicFakeEmbedding and directly redirects the
already-loaded settings singleton to tmp_path. A focused verification completed
with 65 passed and one warning; no OpenAI request was made.

HARD-GATE FIXTURES
Measurement call: check_full_triage(query, query, passages, PLAN). The implementation
requires both original_query and active_query. The current revision-2 plan already
uses the corrected four-argument call.

positive: sufficient=True, triage=sufficient, top_score=0.8,
  keyword_overlap_active=1.0, keyword_overlap_original=1.0, passage_count=2
enumeration: sufficient=True, triage=sufficient, top_score=0.8,
  keyword_overlap_active=1.0, keyword_overlap_original=1.0, passage_count=2
open_ref: sufficient=True, triage=sufficient, top_score=0.8,
  keyword_overlap_active=1.0, keyword_overlap_original=1.0, passage_count=2
zero_overlap: sufficient=False, triage=borderline, top_score=0.8,
  keyword_overlap_active=0.0, keyword_overlap_original=0.0, passage_count=2

TASK 14 PRE-PUSH GATES — BLOCKED 2026-09-10
Tasks 0–13 are complete. Full suite after Task 13: 945 passed, 5 known failed,
7 warnings (950 collected, 43.85 seconds; outside the Codex sandbox).

The route-matrix command was started against the comparable 133-question
baseline `benchmarks/triage_gap_main_133_2026-09-08.json` because the plan's
short alias `triage_gap_main.json` does not exist. It was stopped without an
output artifact: `rag_simple` dense retrieval calls the OpenAI embeddings API,
which returned 429 `credit_balance_exhausted`. The saved benchmark records have
chunk identities but not passage text or retrieval scores, so they cannot be
replayed faithfully through the new triage contract.

The generative eval remains blocked by the same empty OpenAI balance. No route
distribution, quality comparison, or push decision is recorded until both runs
can complete. No push was attempted.

TASK 14 PRE-PUSH GATES — RUN 2026-09-10, QUALITY GATE FAILED

Route matrix (133/133, 0 errors, 0 technical failures): route decisions were
generate=89, complex=43, abstain=1; route reasons were context_sufficient=89,
refs_unresolved=36, triage_borderline=4, enumeration_intent=3, zero_overlap_both=1.
Escalation rate increased from 0.248 to 0.323. HR@12 fell from 0.827 to 0.789:
five baseline hits were lost and none gained. All five losses stayed on the simple
route with reason=context_sufficient and handed 10 passages to generation versus
12 in the baseline. This is consistent with moving the live final-context cap to
MAX_CHUNKS_FOR_LLM=10; the route matrix is diagnostic, not itself the push gate.

Generative eval used the same 56-question dataset and JUDGE_MODEL_NAME=gpt-4o.
New vs variant-B baseline: valid 48/56 vs 53/56; faithfulness 0.894 vs 0.808;
answer relevance 0.748 vs 0.881; correctness all 5.48 vs 7.09; correctness in-scope
5.82 vs 7.40; OOS rejection 100% vs 100%; false-sufficiency 13.8% vs 13.0%;
complex path 39.6% vs 13.2%; p50/p95 5.5/17.8s vs 4.8/14.7s; total cost
$0.2130 vs $0.1739. Seventeen questions changed simple→complex. Five questions
answered by the baseline now produced an empty answer; the most severe was the
program A/B periodicity question (baseline 10/10). Several other former 9–10/10
answers became 3/10 statements that the evidence was missing, including minimum
internship duration and the practical-training share for PPE.

Decision: DO NOT PUSH. The drop is coherent across relevance, correctness, empty
answers, latency, cost, route changes, and named domain-critical examples; it is
not plausibly only judge noise. Next step is a TDD diagnosis of final-context
selection: test the 10-vs-12 simple truncation and preservation of the best direct
evidence across simple→complex escalation, then rerun both Task 14 evaluations.

TASK 14 PRE-PUSH GATES — FIXED 2026-09-11, QUALITY GATE PASSED

Root causes were confirmed by regression tests and live traces: the revived
MAX_CHUNKS_FOR_LLM=10 cut ranks 11-12 that the old bridge actually passed through
under its [:30] cap; complex merging could displace the direct simple norm; and
cross-reference expansion appended structural closers after unrelated neighbours.
The fixes set the explicit cap to 30, anchor the simple top-k ahead of novel complex
passages, and prioritize chunks that reduce the top-5 structural gap before other
expansion output. A narrow refs_best_effort exception now applies only to the last
complex candidate, only after hard gates pass, and only when refs_resolved is the
sole unmet obligation. This handles external citations such as article 213 of the
Labour Code named in the title of Order 29n without weakening the simple path.

Final route matrix: 133/133, 0 errors; escalation 0.195 versus baseline 0.248;
114/133 gaps closed; HR@12 0.827 versus 0.827, with 0 lost and 0 gained hits. Final comparable generative
run (`eval_v7_triage_contract_final_2026-09-11.jsonl`): valid 53/56 versus 53/56;
faithfulness 0.891 versus 0.808; relevance 0.879 versus 0.881; correctness all 7.26
versus 7.09; correctness in-scope 7.47 versus 7.40; false-sufficiency 11.4% versus
13.0%; complex path 17.0% versus 13.2%; p50/p95 4.5/15.7s versus 4.8/14.7s; total
cost $0.2049 versus $0.1739. There are no new in-scope empty answers. Two apparent
per-question judge drops are not dangerous regressions: the evacuation answer does
state the mass-occupancy condition in its rationale, and the instruction-types answer
is materially equivalent to the baseline response.

Decision: QUALITY GATE PASSED. Full suite after the final fixes: 952 passed and the
same 5 known failures. The local series is ready for commit and may be pushed only
after Petr's separate confirmation.
